import { Typography } from '@mui/material';

const Experience = () => (
  <div className="container">
    <Typography variant="h2">Experience Section</Typography>
  </div>
);

export default Experience;
